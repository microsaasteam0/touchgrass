{
  "name": "touchgrass-backend",
  "version": "1.0.0",
  "description": "TouchGrass Backend API",
  "main": "server.js",
  "scripts": {
    "build": "CI=false && npm run build",
    "install": "npm install --legacy-peer-deps",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "Build Command": "npm install",
    "Start Command": "npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "touchgrass",
    "streak",
    "productivity",
    "api"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.6.3",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-rate-limit": "^7.1.5",
    "compression": "^1.7.4",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}